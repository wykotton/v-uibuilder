export enum PageType {
  PROJECT = "project",
  PAGE_WAREHOUSE = "page-warehouse",
  COMPONENT = "component",
  WEBSITE = "website",
  PAGE = "page",
}

export enum PageSpaceType {
  LIST,
  PREVIEW,
}

export enum PageCategory {
  PAGE,
  COMPONENT,
}

export enum UserEventType {
  IMPORT = "IMPORT",
  EXPORT = "EXPORT",
  DELETE = "DELETE",
  REFRESH = "REFRESH",
}
